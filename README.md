# PingDrop - Frontend Intern Assignment

## Run locally

npm install
npm run dev

## Notes

- Uses React Context (src/context/ChatContext.tsx) to store chats.
- State persisted to localStorage.
- Assistant replies are simulated placeholder messages for demo.
